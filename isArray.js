/* IS ARRAY
- checks if the passed value is an array
- lives on the Array class 
*/

const arr = ["Florin", "Ivan", "Liam"];
const str = "hello world";
const number = 7;

console.log(Array.isArray(arr)); // returns true
