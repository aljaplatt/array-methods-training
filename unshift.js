/* UNSHIFT 
- Adds 1 or more elements to the start of an array
- returns new array length
- add in the order you want -1, 0, not 0, -1 etc 
*/

const numbers = [1, 2, 3, 4];

// numbers.unshift(0);
numbers.unshift(-1, 0);

console.log(numbers);
